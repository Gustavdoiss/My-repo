#include <stdio.h>

int main() {

    int ct, n, s;
    scanf("%d", &ct);

    for (int i = 1; i <= ct; i++) {
        scanf("%d %d", &n, &s);
        int circle[n];
        for (int j = 0; j < n; j++) {
            circle[j] = j + 1;
        }
        indice = (indice + 1) % length;
        int tam = n, cont = n;
        while (tam > 1) {

            tam--;
        }
    }

    return 0;
}
